import { HideLinkForDescriptionDirective } from './hide-link-for-description.directive';

describe('HideLinkForDescriptionDirective', () => {
  it('should create an instance', () => {
    const directive = new HideLinkForDescriptionDirective();
    expect(directive).toBeTruthy();
  });
});
